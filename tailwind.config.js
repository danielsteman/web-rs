module.exports = {
  content: ["./templates/**/*.{html,js}"],
  theme: {
    extend: {
      // Customize the list styles
      listStyleType: {
        // Apply the custom style to all lists by default
        default: "disc",
      },
    },
  },
  plugins: [],
  darkMode: "class",
};
